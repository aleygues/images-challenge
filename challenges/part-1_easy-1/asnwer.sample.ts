/**
 * Your function should compute a string from an array of bytes
 * One byte represents one letter (UTF8 encoding)
 * For instance, this input: [117, 192]
 * Should produce: "uÀ"
 * @param bytes is an array of bytes (numbers from 0 to 255)
 * @returns a string computed from the bytes array, using UTF8
 */

export default function (bytes: number[]): string {
  return "";
}
