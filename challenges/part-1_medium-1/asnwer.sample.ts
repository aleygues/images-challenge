/**
 * Your function must convert a byte (a number between 0 and 255)
 * to its corresponding 8 bits
 * For instance, 117 (byte) is 01110101 (bits)
 * You should avoid using `parseInt` and `toString` methods, use additions, multiplications and powers instead!
 * You can check this https://pixees.fr/informatiquelycee/n_site/isn_base_2.html and https://tube-numerique-educatif.apps.education.fr/w/8j9bzV3912WB2JCdowx6Yj if needed
 * @param byte to convert
 * @returns an array of 8 bits
 */

export default function (byte: number): (1 | 0)[] {
  const bits: (1 | 0)[] = [];
  return bits;
}
